1) First of all install express module of NodeJs using npm globally and localy (in the same folder).
2) Run myStaticserver using commond "node myStaicserver" and hit enter.
3) Now server get start with port number "Localhost:3000"
